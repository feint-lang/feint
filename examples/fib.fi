fib = (n) ->
    if n == 0 -> 0
    else if n == 1 -> 1
    else -> fib(n - 1) + fib(n - 2)

# TODO: Implement match
# fib_match = (n) ->
#     match n ->
#         0 -> 0
#         1 -> 1
#         : -> fib(n - 1) + fib(n - 2)

$main = (...) ->
    n = if $args.length -> Int.new($args.0) else -> 10
    print(fib(n))
