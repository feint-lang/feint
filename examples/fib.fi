fib_if = (n) ->
    if n == 0 -> 0
    else if n == 1 -> 1
    else -> fib(n - 1) + fib(n - 2)

fib = (n) ->
    match n ->
        0 -> 0
        1 -> 1
        : -> fib(n - 1) + fib(n - 2)

$main = (...) ->
    print(fib(n))
    n = if $args.is_empty() -> 50 else -> Int.new($args.0)
